import React from "react";
import { StyleSheet, Text, View } from "react-native";

const ListScreen = () => {
  return (
    <View>
      <Text style={{ fontSize: 20 }}>list screen</Text>
    </View>
  );
};

export default ListScreen;

const styles = StyleSheet.create({});

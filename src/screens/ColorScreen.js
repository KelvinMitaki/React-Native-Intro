import React from "react";
import { StyleSheet, Text, View } from "react-native";

const ColorScreen = () => {
  return (
    <View>
      <Text>ColorScreen ColorScreen</Text>
    </View>
  );
};

export default ColorScreen;

const styles = StyleSheet.create({});
